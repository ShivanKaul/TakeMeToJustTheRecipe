{
  "name": "TakeMeToJustTheRecipe",
  "version": "0.0.1",
  "description": "Open this page in justtherecipe.app",
  "main": "",
  "scripts": {
    "deps": "npm install",
    "stage": "mkdir ext/dist || echo Dist already exists.",
    "clean": "rm -rf ext/dist",
    "bundle": "browserify -o ext/dist/background.js ext/src/background.js",
    "build": "npm run clean && npm run stage && npm run bundle"
  },
  "author": "Shivan Sahib",
  "license": "MIT",
  "devDependencies": {
    "browserify": "16.5.2"
  }
}
